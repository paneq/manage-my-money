
<% controller = @category ? :categories : :transfers %>
<% form_remote_tag :url => {:action => :search, :controller => controller } do %>

  <% if @category %>

    Uwzględnij podkategorie &nbsp;&nbsp;<%= check_box_tag 'include_subcategories', true %>
    <%= hidden_field_tag :id, @category.id %>

  <%  end %>

  <%= date_period_fields("transfer_day", (@start_day || Date.today), (@end_day || Date.today)) %>
  <%= submit_tag '&nbsp; Szukaj &nbsp;' %>

<% end %>
