
<% current_category ||= nil %>
<% controller = current_category.nil? ? :transfers : :categories %>
<% form_remote_tag :url => {:action => :search, :controller => controller } do %>

  <% unless current_category.nil? %>

    Uwzględnij podkategorie &nbsp;&nbsp;<%= check_box_tag 'include_subcategories', true %>
    <%= hidden_field_tag :id, current_category.id %>

  <%  end %>

  <%= date_period_fields("transfer_day", @start_day, @end_day) %>
  <%= submit_tag '&nbsp; Szukaj &nbsp;' %>

<% end %>
